# Qwen3.8-27B DFlash2 Op 修正与重新 qualification checklist

> 状态：进行中，OP-01..03 已完成，OP-04..37 待完成。首轮实现中的多个合同和索引固定了推荐的七个 draft token。
> 本清单管理正式 Engine 接入前的 `K=1..15` 治理；各项完成情况和证据在对应条目内记录。
>
> 算法由
> [`2026-09-04-qwen3.8-27b-dflash2-algorithm.md`](2026-09-04-qwen3.8-27b-dflash2-algorithm.md)
> 定义，跨阶段集成由
> [`2026-09-04-qwen3.8-27b-dflash2-support-plan.md`](2026-09-04-qwen3.8-27b-dflash2-support-plan.md)
> 管理，权重与 format 以
> [`qwen3.8-27b-artifact.md`](qwen3.8-27b-artifact.md) 为准。

## 1. 范围与完成含义

当前问题是将 checkpoint 推荐配置 `block_size=8` 误用为 Op 数学域、kernel 索引和性能域。
本轮要完成受影响合同、wrapper、workspace query、dispatch、kernel、独立 oracle 和 benchmark
的对应修正，并对已有合法 routes 在新产品点重新 qualification。所有实现继续由原 Op family
拥有，不添加 target-private 平行路径，也不按 K 建立不同的 public Op。

本文逐项列出 37 个 Op/profile 工作项。OP-01..08 必须修改合同/实现并重新 qualification；
OP-09..32 已具有所需 extent，需复核数值/状态与性能并按证据修改原 routes；OP-33..37 为
准备、提交相关的直接复用项，重点验证新宽度的可观察行为，不要求无依据地重写或调优。
表格中一个 Op 的多个已注册格式均属于该项，关闭时分别说明其验证结果。

旧 K=7 的实现和测量可作为同点基线，不能沿用“全域已完成”的结论。下文列出的现有阈值
用于定位待比较的分界，不是重新调优后的固定方案。没有测量支持时，不宣称某个候选更快。

正式 Engine schedule、binder 接线、Program state/workspace composition、Graph inventory 和
端到端 acceptance/吞吐属于后续集成。本文只要求 Op 自身在代表性合法输入上可独立调用、
验证和 capture/replay。改变运行 K 不要求重新下载 source、转换 artifact、repack 权重或
引入新的量化格式。

## 2. 统一 workload、索引与数值边界

### 2.1 运行 extent

| 符号 | 含义 | 支持范围 |
|---|---|---|
| B | active requests | 1..8 |
| K | 启动选定的 draft count | 1..15，推荐 7 |
| W | proposal/verify 物理 block width | K+1，2..16 |
| T | backbone/target matrix columns | W*B，最大 128 |
| U | mask-head/selector matrix columns | K*B，最大 120 |
| P_b | 每请求可验证 draft extent | 0..K |
| Q_b | target live input columns | P_b+1 |
| A_b | accepted draft count | 0..P_b |
| L_b | provisional output count | A_b+1 |
| N_b | Frontend 最终提交数 | 0..L_b |
| W_c、B_c | context materialization 物理布局 | decode W_c=1..16、B_c=1..8；prefill W_c=1..2048、B_c=1 |
| T_c | context projection columns | W_c*B_c；prefill 最大 2048 |
| N_lane | pending-feature lane capacity | startup-fixed 1..8，且 >=B |
| S_state | StateImage slot capacity | startup-fixed，覆盖本轮 state selectors |

同一 Engine 的物理 W 由选定 K 决定；不能为全部配置计算有效 W=8 或 W=16 来规避扩展。
Proposal attention 可使用 valid_columns=W，target attention/GDN 使用 Q_b，context append
使用 counts=N_b。W_c 是 pending storage/gather extent，不能由 accepted draft count 替代；
all-accept 会提交 K+1 个 target input rows，必须保留最后一行 feature。

性能输入域以 `{(K+1)*B}` 和 `{K*B}` 的实际乘积集合定义。纯矩阵 Op 可以按相同列数去重；
卷积、attention、GDN 和状态 Op 必须保留 W/B 划分以及 device counts。已有 Linear 类 Op 的
任意正 T 合同继续成立；最大 128/120 是本轮产品性能范围，不是这些合同的新上限。

### 2.2 固定模型 geometry 与张量布局

```text
hidden H=5120; MLP I=17408; target feature H_f=25600
draft Q=32*128=4096; K/V=8*128=1024; five local layers; window/capacity=2048
conv groups=320; group width=16; taps=2; sides=2
selector candidates M=16; rank R=256
physical vocab=248320; valid global ids=0..248076; optimized head rows=131072

proposal residual       BF16 [5120,W,B]
finish_delta            BF16 [320,2,W,B]
packed mask hidden      BF16 [5120,U]
candidate ids/scores    I32/FP32 [16,K,B]
projected selector h    BF16 [256,K,B]
drafts / proposal q     I32 [K,B] / FP32 [16,K,B]
target logits           BF16 [248320,W,B]
pending features        BF16 [25600,W,N_lane]
```

Artifact Weight `[N,C]` 表示 `[output rows,input columns]`；runtime Tensor 的 ne[0] 连续。
Base kernel 保留 artifact `[side,tap,channel]=[2,2,5120]`，runtime view 为 `[5120,2,2]`；
codebook 保留 artifact `[vocab,rank]`，runtime view 为 `[256,248320]`。这些固定 8/16/256
等模型维度不能随运行 W/K 一起替换。

### 2.3 保持的数学与实现自由度

- Dynamic convolution 仅在当前 request block 内跨相邻位置，位置零没有 previous tap；
  finish 消费同一 prepare 的 output-side delta，无跨轮卷积状态。
- Proposal SWA 为 `abs(key_position-query_position)<2048`；当前 block 的有效 query 互相
  可见。Q/K 使用 plain head RMSNorm 和 theta=1e7 的 full-D128 split-half 1-D RoPE。
- Final norm-pack 跳过 anchor；top-16 使用 score 降序、同分 global-id 升序。Optimized
  head 的 row id 先映射为 global id，再供 selector 使用。
- Selector 是实际前驱条件下的逐位 walk；保留实际抽样的 FP32 q。逐 row、部分预计算、
  完整 lattice 均可作为同一 Op 的内部方案，不能换成独立逐位置 unary argmax 或 Viterbi。
- Sparse accept 使用实际 q 的 p/q rejection 和 `(p-q)+` correction；N 而非 P/A/L 决定
  最终持久状态提交。Proposal RNG purpose 与 target accept/correction/bonus 分离。
- Norm/conv prepare、projection/conv/residual、norm/RoPE、norm/pack、projection/top-k、
  selector walk 和五层 context materialization 保持闭合 public 边界。Private staging、
  reduction、tiling、launch 数由 qualified route 决定，不要求每个融合 Op 只有一个 kernel。
- Oracle 从 represented public inputs 及精确解码的 stored weights 计算完整公式；不可观察
  中间精度作为 implementation profile。OP-07 要去掉 raw K 的额外语义舍入；最终 BF16 K、
  `FP16_RNE(BF16)` V storage cast、public BF16 outputs 和实际 FP32 q 保持各自边界。

## 3. 必须修正合同和实现的 Ops

### OP-01 — rmsnorm_dynamic_grouped_conv_prepare

合同：[dynamic_grouped_conv.h](../../include/ninfer/ops/dynamic_grouped_conv.h)。落点：
`src/ops/wrapper/dynamic_grouped_conv.cpp`、`src/ops/dynamic_grouped_conv/bf16/`。

已修正的问题：合同和 wrapper 固定 W=8；plan 仅按 B 选择 split-K。Partial kernel 将八列 MMA tile
与 request batch 绑定，reduce 的 128 threads 正好只处理 8*16 个位置/通道。

- [x] 将 residual/prepared 改为 `[5120,W,B]`、finish_delta 改为 `[320,2,W,B]`，支持 W=2..16；
  workspace query 显式接收宽度域和 batch 域，和执行共用 route/layout 事实。
- [x] 分离 contraction 的 T 分片和卷积的 W 边界，改齐 partial/reduce 索引、shared storage、
  非满 tile 和线程覆盖；比较 split-K、norm staging/融合及 coefficient reduction 方案。
- [x] 从完整 norm→projection→input-conv 公式验证两个 public outputs；覆盖所有 W、不同 B、
  非零前一 request 末列和新 route seams；测完整 Op 的 launch 与 workspace 成本。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。独立 FP64 oracle 覆盖全部 120 个 W/B
组合；prepared/finish_delta 的既有 relative-L2/gross criteria 不变，输出 guard、输入保留、
scope 归还及 query high-water 均通过。W/B=3/3、7/7、14/7、16/8 另通过同址双次 Graph replay。

比较了六组 tiling/split 方案及其 RMS 缩放因子后置的两节点融合版本，共12个完整 Op 候选。
两种算术 profile 均先通过 oracle；保留 norm staging 的三节点方案更快，未采用的实例和
临时 sweep 接口已移除。最终 T<=48 使用 R16、ceil(T/8)*8 列、split-8；49..96 使用
R32/C32/split-4；97..128 使用 R32/C64/split-4。最大 workspace 为 2,621,440 bytes。

Public benchmark 已改为实际捕获 Graph，使用256 MiB L2 flush、8 warmup/60 repeats，遍历
全部 W/B。完整 Op median 如下；这是 Graph 测量，不能与旧 benchmark 的 eager-event 值混比。

| W | B=1 median us | B=8 median us |
|---:|---:|---:|
| 2 | 15.648 | 17.664 |
| 8 | 17.664 | 25.408 |
| 16 | 17.664 | 32.000 |

同条件下，B=8/W=9、12、16 相比直接扩宽的 R16 候选，从29.952/32.512/38.144 us
降至25.856/27.904/32.000 us。全域同 B 的相邻 W 最大增长为2.048 us；48→49列为
23.808→23.808 us，96→下一实际列数98为27.904→29.952 us，与候选曲线一致。
复现：`build/tests/ninfer_dynamic_grouped_conv_prepare_test` 和
`build/bench/ninfer_dynamic_grouped_conv_prepare_bench --warmup 8 --repeat 60`。

### OP-02 — linear_dynamic_grouped_conv_add

合同同 OP-01。落点：`src/ops/dynamic_grouped_conv/w8/` 和公共 wrapper。

已修正的问题：两个 C=4096/17408 profile 的 scratch、实例表和 dispatch 都基于 8B；finish 用
`col & 7` 判断 previous tap。旧 B-specific cache/K-warp 表不能覆盖新 W/B geometry。

- [x] 支持 x `[C,W,B]`、residual `[5120,W,B]` 和匹配 delta；按真实 request 内位置断开 tap。
  Query 覆盖两个 C profile 及全部选择域，不能仅将旧 capacity 乘二。
- [x] 重选小 T 与 65..128 列的 contraction；比较 private materialized projection 与融合
  finish epilogue 的完整 Op 成本。Raw projection 的 BF16 staging 仍是实现 profile。
- [x] 独立解码 signed W8/FP16 scales 后计算完整公式，检查 residual 原地更新、跨 request
  边界和 scope 归还。若采用 materialized route，原始 payload 为 `5120*W*B*2` bytes，
  最大 1.25 MiB；有额外 scratch 的其他 routes 分别计入实际 query。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。两种 C 各覆盖120个 W/B 组合；27个
跨 group、MMA tile 和矩阵区间的代表性输出行直接对完整 FP64 projection→conv→residual
oracle 比较，原 numerical criterion 不变。输出与精确容量 workspace 的 guards、输入/权重
保留和 scope high-water 检查通过。各最终 route 的代表点另验证同址双次 Graph replay，
每次 replay 前恢复 residual；共享 primitive 的 W8 LinearSwiGLU 和 attn_input_proj tests 通过。

比较了完整融合 epilogue、原 small-T 的分次 projection、普通 MMA、单次 launch 的并行
column tiles 和更宽的完整 tiles。Fusion 没有稳定收益，生产保留两节点 materialized Op；
移除了分次 projection 的65列台阶及未采用的 sweep/instances。W8 primitive 现在支持带
尾列屏蔽的二维 column grid，并统一拥有 shared-storage 类型；宽 tile 使用显式 shared
memory opt-in。88列 tile 的49,408-byte shared storage仍容纳两个CTA，96/128列候选未胜出。

最终两个 C profiles 均在 T<=64 使用 exact-small-T，65..72/73..80/81..88 使用
72/80/88列 tile。C=4096 的89..96使用并行32列 tile，97..128使用R64/C64/K128/A1；
C=17408 的89..128直接使用后者。所有 routes 使用同一 `[5120,W,B]` BF16 scratch，
最大1,310,720 bytes；finish 始终按真实 W 重置 previous tap。

Public cold-cache Graph benchmark：256 MiB L2 flush、8 warmup/60 repeats，全部240个
C/W/B点，均为2个Graph nodes。代表性完整 Op median：

| C | W | B=1 us | B=8 us |
|---:|---:|---:|---:|
| 4096 | 2 | 19.744 | 21.760 |
| 4096 | 8 | 21.760 | 40.192 |
| 4096 | 16 | 21.856 | 62.720 |
| 17408 | 2 | 66.848 | 69.376 |
| 17408 | 8 | 68.672 | 165.856 |
| 17408 | 16 | 69.408 | 236.800 |

C=17408 的64→65列为165.856→173.312 us，分次投影候选在65列约218..222 us；
T=128为236.800 us，分次投影候选约331 us。C=4096的相同最大点为62.720 us，分次
投影候选约77 us。推荐W=8的代表点保持在原测量波动范围。
同B相邻W最大增长：C=4096为B8的88→96列，50.432→58.624 us；C=17408为B8的
48→56列，约26.208 us增长。前者跨过两CTA宽tile的资源边界，后者包含8列计算量增长；
上述点的候选比较未提供更快且连续的替代，不声称全曲线无台阶。
复现：`build/tests/ninfer_linear_dynamic_grouped_conv_add_test` 和
`build/bench/ninfer_linear_dynamic_grouped_conv_add_bench --warmup 8 --repeat 60`。

### OP-03 — rmsnorm_rope（pair；single 作为 context 依赖）

合同：[rmsnorm_rope.h](../../include/ninfer/ops/rmsnorm_rope.h)。落点：`src/ops/rmsnorm_rope/`。

已修正的问题：Pair contract/wrapper 固定 `[128,32/8,8,B]` 和八列 positions；底层 launcher 已接受
token 数。Single T=1..2048 已覆盖 context，不能因改 pair 而破坏其范围。

- [x] Pair 使用真实 W 和 W*B，接纳 W=2..16、B=1..8，维持 Q/K 原地写入与 positions 对齐。
- [x] 用完整 norm→RoPE FP64 oracle 验证新 pair extent、绝对大位置及 single 的受影响路径；
  比较每 token CTA 的 head 分工在短 W 和大 batch 的代价，保留有证据的方案。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。Pair 全部120个 W/B 组合及single
T=1/8/64/1024/2048直接通过完整FP64 oracle，原relative-L2和pair-scaled准则不变；
positions=0和接近262144的绝对位置、输入weight/positions保留及输出guards通过。
Pair W/B=3/3、16/8和single T=1/2048另通过恢复输入后的同址双次Graph replay。

比较6种pair与4种single head/warp分工。最终每CTA处理同一token的8个head、每warp一个
head；pair用4个Q CTA和1个K CTA，single保留1个K CTA，每CTA只缓存自身的norm weight。
仍为一次public调用、一个kernel、零workspace；未采用的分工和临时sweep已删除。

Public benchmark采用warm cache、8 warmup/60 repeats，每个Graph串联32次Op并按次数归一化，
避免逐次host launch间隙主导微秒级Op测量。Pair代表性median：

| W | B=1 us | B=8 us |
|---:|---:|---:|
| 2 | 2.015 | 2.023 |
| 8 | 2.001 | 2.185 |
| 16 | 2.020 | 2.429 |

同条件原40-head/CTA候选在W8/B1、W16/B8为3.242/3.294 us；最终为2.001/2.429 us。
全部pair点同B相邻W最大增长0.093 us，无新增route seam。Single T=1/128/1024/2048为
1.981/2.075/3.029/4.074 us，没有引入针对噪声点的切换阈值。
复现：`build/tests/ninfer_rmsnorm_rope_test`，以及
`build/bench/ninfer_rmsnorm_rope_bench --form pair --warmup 8 --repeat 60`（single改用`--form single`）。

### OP-04 — rmsnorm_pack_tail

合同：[rmsnorm_pack_tail.h](../../include/ninfer/ops/rmsnorm_pack_tail.h)。落点：
`src/ops/wrapper/rmsnorm_pack_tail.cpp`、`src/ops/launcher/rmsnorm_pack_tail.cu`、
`src/ops/kernel/rmsnorm_pack_tail.cuh`。

问题：输入 stride=8、输出 stride=7、grid 和 oracle fixture 均写死推荐 block。

- [ ] 定义 `[5120,W,B] -> [5120,(W-1)*B]`，按 `out[:,K*b+i-1]=norm(in[:,i,b])`
  处理 i=1..K；使用真实 W/K，anchor 不参与读取、归约或输出。
- [ ] 用独立 norm/索引 oracle 覆盖短、奇数、最大 W 和跨 request pack；复核 CTA/thread
  配置与完整 launch 时间，不额外物化一份完整 normalized block。

### OP-05 — linear_topk（三个 head profiles）

合同：[linear_topk.h](../../include/ninfer/ops/linear_topk.h)。落点：`src/ops/linear_topk/`。

问题：Wrapper 从 U%7 推断 B，workspace 只查询 B；kernel 只覆盖旧 `7,14,...,56` 集合。
W8/FP8/Q4 的若干 launcher 在未命中时直接调用固定 56 列实例，放宽 wrapper 会错误读写。

- [ ] 将 public contract 定义为逐矩阵列独立的 projection-top16，hidden `[5120,U]`，
  outputs `[16,U]`；caller 可 view 成 `[16,K,B]`。列轴支持任意正 extent，workspace 按
  column interval 查询；本轮产品优化域为 U=K*B<=120。
- [ ] 分别覆盖 W8/row-FP8 full `[248320,5120]` 和 Q4 optimized `[131072,5120]`；
  重建完整 dispatch、masked tail、column tiling 和 merge，删除将任意列数送入旧 exact
  instance 的分支。不得只补 U=120 或保留固定七列的平行 public overload。
- [ ] 比较小 U K-split、grouped row reduction、更大 U MMA 和 merge。当前 full U=7
  producer 每 16 个 vocabulary rows 写 16 个 64-bit keys，partial plane 仍约 13.26 MiB；
  Q4 U=7/14 也逐 vocabulary row 写 key。计入实际流量，不把“不公开 dense logits”当作
  已实现内存压缩的证据；比较更大 row group 内先选 top-16 的完整开销。
- [ ] 三个 profiles 各自用精确 weight decode 的独立数学 oracle 验证 scores、global-id
  tie、padding 排除和 id map。浮点边界按数值准则，精确 tie/构造的稳定候选按 exact 准则；
  补全部新增 route、非整 tile、workspace 峰值与同址 Graph replay。

### OP-06 — candidate_selector_path

合同：[candidate_selector.h](../../include/ninfer/ops/candidate_selector.h)。落点：
`src/ops/wrapper/candidate_selector.cpp`、`src/ops/candidate_selector/bf16/`。

问题：合同、strides 和 kernel 七步展开固定 K=7；每 request 一个 512-thread CTA 的调度
只在旧宽度有证据。原先将“只算 realized row”固定为唯一 production 方案限制了比较范围。

- [ ] 从输入读取 K=1..15，改齐 `[16,K,B]`、`[256,K,B]`、`[K,B]` 的索引和循环；
  保留固定 M=16/R=256，以及 `(seed,F+i,DFlash2ProposalPurpose)` 的位置 RNG。
- [ ] 比较逐步 realized-row、提前 staging、部分/完整 edge 预计算后的条件 walk；包括
  K=1 的单步和 K=15 的串行链。若新增 private workspace，提供匹配 query；不预定赢家。
- [ ] 独立 FP64 lattice oracle 再执行条件 walk，检查实际前驱下的 q；覆盖混合温度、
  greedy tie、依赖前驱而改变选择的案例和 compact-row 重排下的 RNG。q 输出必须就是
  实际抽样使用的 FP32 分布，不从其他 route 的 scores 重建。

### OP-07 — context_kv_materialize

合同：[context_kv_materialize.h](../../include/ninfer/ops/context_kv_materialize.h)。落点：
`src/ops/context_kv_materialize/`。

问题：B>1 只接纳 W_c=8，其他宽度仅 B=1 composed 合法。Direct 的四列 tile 用平坦 col0
推断 request，grid 固定 2*B；仅替换 W 常量会在非四倍数 W_c 下漏列。Raw K 的 BF16
舍入还被写入 public formula 和 oracle，限制 projection→norm→RoPE 的内部融合。

- [ ] 支持 batched W_c=1..16、B_c=1..8 和原单请求 W_c=1..2048。保持五组独立 QKV-parent
  K/V row views、norm 和容量 2048 的 cyclic caches；不拼接 persistent 权重。
- [ ] 将 tile 显式划分为 request 与 request 内 tile，或提供等价的逐列边界处理；改齐
  projection/post/store、layer-major scratch、composed batch views 和 width interval query。
  Counts/envelope 确定有效写入，不把物理 padding 写进 ring。
- [ ] 从合同/oracle 去掉不可观察 raw K 的强制 BF16 rounding，以完整 projection→norm→RoPE
  公式直接验证最终 K；现有 BF16 staging 可以作为 qualified implementation profile。
  明确保留最终 BF16 K、`FP16_RNE(BF16)` V storage cast 和无效位置的 exact 无副作用。
- [ ] 比较 grouped SIMT/MMA、projection+K-post/store、进一步融合和 composed routes；
  按 W_c/B_c/count envelope 选择，避免把 W_c=8 当作 decode 的唯一标记。
- [ ] 状态 oracle 覆盖 W_c=1..16、counts=0/短/满、奇数 W_c、lane 重排、2048 wrap、
  大绝对位置及有变化的 prefill routes。Query 覆盖 route 转换的真实最大 scratch，Graph
  replay 改变 counts 后仍不读写无效 state。Ring 持久 payload 不随 K 翻倍。

### OP-08 — speculative_accept_sparse_drafts

合同：[speculative_round.h](../../include/ninfer/ops/speculative_round.h)。落点：
`src/ops/wrapper/speculative_round.cpp`、`src/ops/launcher/speculative_round.cu`、
`src/ops/kernel/speculative_round.cuh`。

问题：Sparse wrapper、raw-greedy kernel、launcher 和 workspace 固定 K=7/W=8，虽然
general sampling substrate 已有动态 K。仅放宽输入 shape 会产生错误 stride、bonus 列和容量。

- [ ] 接纳 K=1..15、B=1..8；从 drafts 的 extent 推导 W，改齐 target logits/tokens、
  candidate/q、extent clamp、licensed outputs 和尾部清零。Capacity query 覆盖 K/B 域；
  raw-greedy envelope 仍允许零 workspace。
- [ ] 在原 acceptance family 内复用动态 K sampling substrate；保留 actual FP32 sparse q、
  token domain=248077、最多 16 个 proposal candidates 以及 target 的 filtering/penalty 合同。
  Token counts 只读，length/anchor 是 provisional buffers。
- [ ] 独立 p/q oracle 覆盖 P=0、首/中/末位拒绝、all-accept bonus、q support 与 p 不同、
  mixed greedy/stochastic 和 penalty history。结果验证包含 A、L、tokens、provisional
  length/anchor、未提交 token counts 和无效物理尾；比较 raw/general 两类完整 route。

## 4. 已支持 extent、需要重新 qualification 的逐 Op 清单

这些项目不因为重新 qualification 就必须修改合同或 kernel。若测量要求改 route，应在原
family 完成实现和独立 oracle 验证。每项的“待复核”在相关格式和新 workload 证据齐备后
才改为完成；未改动且已有证据直接覆盖的点引用已有结果，不重复实验。

### 4.1 Context 与 proposal 主干

| ID / 状态 | Op 与合同 | 精确 profile / 新输入 | 复核与方案 |
|---|---|---|---|
| OP-09 待复核 | [linear](../../include/ninfer/ops/linear.h) feature projection | W8 `[5120,25600]`，decode T_c<=128，prefill<=2048 | 当前 T<=64 small-T、T>64 使用按 prefill 选出的 R64/C128；比较新增 65..128 的 MMA/分片，验证 64/65 seam。保留任意正 T 合同。 |
| OP-10 待复核 | [linear](../../include/ninfer/ops/linear.h) selector-hidden | BF16 `[256,5120]`，U<=120 | 当前 U>64 按 64 列串行 launch；比较单次 grid/合适 tile 与两次 launch 的完整成本，验证 BF16 输出及 64/65、非整 tile。 |
| OP-11 待复核 | [attn_input_proj](../../include/ninfer/ops/attn_input_proj.h) draft QKV | W8 parent `[6144,5120]`，直接输出 Q/K/V，T<=128 | 已支持正 T；复核 small-T 53/54、64 列分片及较大 T 的 route。三输出均直接对数学 oracle，不引入 public packed QKV。 |
| OP-12 待复核 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) draft MLP | W8 `[34816,5120] -> [17408,T]`，T<=128 | 已支持正 T；比较 51/52、63/64/65、96/97 和实际内点，完整 gate/up→SwiGLU oracle。固定 64 列特例是否保留由新证据决定。 |
| OP-13 待复核 | [sliding_window_attention](../../include/ninfer/ops/sliding_window_attention.h) | D128/Q32/KV8、window=2048，W=2..16、B=1..8 | 已接纳 W<=16；复核旧 96-context direct/split 分界、KeyBlock、split 数和合并代价。Oracle 检查对称窗口的 2047/2048 距离边界、query 右侧可见集及 ring wrap。 |
| OP-14 待复核 | [kv_cache_append_prefix](../../include/ninfer/ops/kv_cache_append.h) cyclic form | BF16 K/V `[128,8,W_c,B_c]`，capacity=2048 | 已支持正 extent；验证奇数 W_c、counts=0/短/满、state slots 和 wrap，K exact copy/V storage cast、无效尾无写入。测量采用真实 counts；保留既有 4096 route 的受影响行为。 |
| OP-15 待复核 | [linear_pair](../../include/ninfer/ops/linear_pair.h) | 两个 W8 `[1024,5120]`，QKV-parent row views，T_c | OP-07 composed 的内部依赖；新 decode 可跨旧 85/86 SIMT/MMA seam。只对最终仍可达的 composed points 和受影响 prefill route 补 oracle/性能比较，不强制保留 composed 为生产赢家。 |
| OP-16 待复核 | [prepare_ragged_prefix](../../include/ninfer/ops/prepare_ragged_prefix.h) | source `[25600,W_c,N_lane]`，destination `[25600,W_c,B]` | 已有 generic copy 合同；exact 验证 counts=N、lane 重排、零/满前缀、positions 和尾部 zero-fill。W_c 最大16，不能按 K 丢掉 all-accept 的末行。 |
| OP-17 待复核 | [embedding](../../include/ninfer/ops/embedding.h) | W8/row-FP8 `[248320,5120]`，proposal/target T<=128 | 复核真实 ids 和新列数的逐行 decode、尾 tile 与输出；权重 format 的 oracle 保持不变。 |
| OP-18 待复核 | [rmsnorm](../../include/ninfer/ops/rmsnorm.h) | context plain D5120、T_c；target offset D5120 及 Q/K D256、T<=128 | 明确 draft/context plain gain 与 target offset gain；补相关 extent/reduction 行布局。性能按实际调用规模复核，不将所有 norms 写成 plain 或只测 D5120。 |

### 4.2 Target verify 与 feature capture

Target 是 64 层：16 个 full-attention、48 个 GDN、64 个 MLP。工作域从旧主要关注
T=56/64 的点扩为 W=2..16、B=1..8。Target full attention 的 Q/gate 宽度为 6144，
K/V 宽度 1024；GDN 为 Hq=16/Hv=48、D=128、FP32 persistent recurrent state。
Groupwise 和 nvfp4 的实际格式分别按 artifact binding 使用；nvfp4 中末八层 MLP 的 FP8
profile 也在范围内。不能把别的 target 的相似 geometry 测量当作本表的全部证据。

| ID / 状态 | Op 与合同 | 精确 profile / 新输入 | 复核与方案 |
|---|---|---|---|
| OP-19 待复核 | [scatter_bf16_batch](../../include/ninfer/ops/scatter.h) | 五次 `[5120,W,B]` capture 写入 `[25600,W,N_lane]` slices | 检查目标 view 的 stride、五个 feature offset、lane 和 valid columns；exact copy、不覆盖其他 slice/无效位置；测完整 capture 成本。 |
| OP-20 待复核 | [attn_input_proj](../../include/ninfer/ops/attn_input_proj.h) target form | Q4/Q5 与 row-FP8 target profiles，T<=128 | 复核新列数的 grouped projection、A16/A8 路径、column tile/尾部和 workspace；Q/gate/K/V 均对各 represented profile 的 oracle。 |
| OP-21 待复核 | [causal_softmax_attention](../../include/ninfer/ops/softmax_attention.h) | D256/Q24/KV4，W=2..16、B=1..8，已注册 target cache codecs | 重点：H24 B1 的特殊 chunked 选择目前仅 W<=8，W=9..16 会走 Prompt。比较这些宽度的短/长 context 和完整 append-and-attend，重选 Prompt/ChunkedSmallT 或改进 verify route；valid columns、causal mask 和 KV codec 一并验证。 |
| OP-22 待复核 | [gdn_norm_gating_proj](../../include/ninfer/ops/gdn_gating_proj.h) | D5120 norm + BF16 control `[96,5120]`，g/beta FP32 `[48,W,B]` | 已接纳正矩阵列数；补 T<=128 和 small/MMA split seam，完整 norm→control→gate/beta oracle，保留 FP32 public outputs。 |
| OP-23 待复核 | [gdn_input_proj_conv_record](../../include/ninfer/ops/gdn_input_proj.h) | Groupwise Q4/Q5 双 parent、row-FP8 `[16384,5120]`；W<=16/B<=8 | 已有 ReplaySSM 域。复核 aggregate 15/16 的 grouped seam、FP8 B1 W=9/10 的 fused/A8 seam及新增大 batch 列数；验证 request-local conv history、valid columns 和 raw conv records，无 persistent state 提前提交。 |
| OP-24 待复核 | [gated_delta_net_replay_record](../../include/ninfer/ops/gated_delta_net.h) | BF16 Q/K `[128,16,W,B]`、V/out `[128,48,W,B]`，FP32 g/beta/state | 已支持 W=2..16/B=1..8；覆盖不同 Q_b 的 FP32 recurrence、raw K/V/gate records 与无效后缀，保持 source state 不变。比较长 W 的串行 recurrence、launch 分工和 record 写入成本。 |
| OP-25 待复核 | [gated_rmsnorm](../../include/ninfer/ops/gated_rmsnorm.h) | D128、48 heads、W*B<=128 的 recurrent output/z | 完整 norm/gate 公式及 head/token flattening；复核新增 extent 的线程布局与内存吞吐，不能用单纯 RMSNorm 的证据替代。 |
| OP-26 待复核 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) target MLP | Q4、NVFP4、row-FP8 `[34816,5120]`，T<=128 | 分别覆盖实际绑定格式和 activation policy。重点复核 Q4 materialized、大 T NVFP4 M64/M96/97+ 切换、末八层 FP8；比较完整 fused/composed 成本并直接验证完整公式。 |
| OP-27 待复核 | [linear_add](../../include/ninfer/ops/linear_add.h) target output | Q5/row-FP8 `[5120,6144]`；Q5/NVFP4/row-FP8 `[5120,17408]`，T<=128 | Attention/GDN 与 MLP 两个 C profile 分别复核；包括 FP8 A16/A8、Q5 C32 和 NVFP4 W4A4。验证 residual in-place 与实际量化边界，不能只测不带 add 的 Linear。 |
| OP-28 待复核 | [linear](../../include/ninfer/ops/linear.h) target full head | W8/row-FP8 `[248320,5120]`，BF16 logits `[248320,W,B]` | 保持 dense target output 合同。重点复核 W8 64/65 之后 route、FP8 A16 GEMM 和新尾 tile；分别测到 T=128，不能从 proposal top-k 推导本 Op 性能。 |
| OP-29 待复核 | [argmax](../../include/ninfer/ops/argmax.h) | target logits `[248320,W,B]`，valid_rows=248077 | 验证所有列、padding 排除和最小 global-id tie；复核 T<=128。只在 acceptance envelope 允许时作为 raw-greedy target 输入。 |
| OP-30 待复核 | [rope](../../include/ninfer/ops/rope.h) target form | Q/K D256、rotary_dim=64，W<=16/B<=8 | 补真实 target position tensor 布局、逐 lane offset 和 partial MRoPE 数值；DFlash2 的一维 D128 RoPE 不替代此 Op。 |
| OP-31 待复核 | [sigmoid_mul](../../include/ninfer/ops/sigmoid_mul.h) | target attention/gate `[6144,W,B]` | 独立 sigmoid-gate 公式、原地输出与新尾 tile；按 16 个 full-attention layers 的调用范围复核性能。 |
| OP-32 待复核 | [offset_i32_positions](../../include/ninfer/ops/position.h) | I32 source/destination vectors 与 scalar delta；target Text/Vision 每 lane 的 W 列位置 | Exact 验证各 lane 调用使用匹配的 vector view/scalar offset，不能将不同 delta 的 batch 当作同一 vector 调用；DFlash2 context/query 的 F+i 绝对 cache position 保持独立。 |

## 5. 准备与提交相关的直接复用 Ops

这些项目仍逐项关闭，避免只扩 proposal 而遗漏 verify/commit 的 K+1 行。没有独立性能疑问
时使用对应行为验证；不为它们附加 benchmark campaign。

| ID / 状态 | Op 与合同 | 新输入与完成条件 |
|---|---|---|
| OP-33 待验证 | [prepare_masked_block](../../include/ninfer/ops/prepare_masked_block.h) | `[W,B]`，W=2..16，mask id=248070；exact 验证 anchor、K 个 masks 和 F..F+K positions，区分 proposal 与 target valid columns。 |
| OP-34 待验证 | [speculative_prepare_verify_ids](../../include/ninfer/ops/speculative_round.h) | drafts `[K,B] -> ids [K+1,B]`，P_b=0..K；exact 验证 anchor/live draft prefix 和安全尾部。若选择同时生成 positions 的同族 form，按同一 alignment 验证，不建立第二份逻辑。 |
| OP-35 待验证 | [speculative_select_accepted_hidden](../../include/ninfer/ops/speculative_round.h) | hidden `[5120,W,B]`，N>0 时 selector=N-1；exact gather 覆盖最大列和非满提交。N=0 的 schedule 不以 -1 调用本 Op。 |
| OP-36 待验证 | [scatter](../../include/ninfer/ops/scatter.h) continuation form | selected hidden `[5120,B]` 写入 owning state slot；exact copy 和 selector 重排。与 OP-35 联合覆盖短提交，保留其他 slots。 |
| OP-37 待验证 | [gdn_replay_fold](../../include/ninfer/ops/gdn_replay.h) prepared Op | 27B 的48个GDN层，records W<=16，commit_columns=N_b；独立状态 oracle 检查 FP32 recurrence 和 conv tail_3，N=0 无读写、N=W 全提交、部分提交与 source/destination slots。这是扩大可提交前缀引入的直接回归点。 |

## 6. Qualification 执行要求

### 6.1 正确性与 route 覆盖

- [ ] OP-01..08 的公共合同、wrapper、执行域和 capacity query 一致。既有 tests/bench fixtures
  改为显式 K/W/B，删除固定推荐宽度是唯一合法输入的断言，不建立兼容 aliases。
- [ ] 对有宽度语义的 Ops，所有支持 W 至少有代表性正确性覆盖；补 B=1/B=8、奇数宽度、
  非满 tile 和相同 T 不同 `(W,B)`，例如 `(W=8,B=2)` 与 `(W=4,B=4)`。矩阵重归约的
  oracle 覆盖每条新生产 route 和其实际分界，不用完整模型输出替代。
- [ ] 状态/稀疏采样项覆盖 device counts 与 physical width 分离、P=0/K、N=0/L、首次/中途
  拒绝和 all-accept；FP32 q 与 token/state effects 分别验证。确定性 codec、copy、索引
  用 exact criterion，浮点公式用声明的数值 criterion，不要求不同 K 产生同一 draft。
- [ ] 每个新增或改变的 workspace route 验证同址 Graph capture/replay、scope lifetime 和
  query 上界。Capacity 取区间内所有可达 routes 的最大值，不能只测最大 K/B 一个点。

### 6.2 性能比较与记录

在 RTX 5090、sm_120a、CUDA 13.1 上排他进行 GPU 计时。编译采用 `cmake --build <dir> -j`。
沿用适用的 public Op benchmark、CUDA Graph 与明确的 cache 条件，计入完整 Op 的所有
launch、scratch 读写和后处理；不把 isolated contraction 的改进直接标成 fused Op 改进。

先用短 K、推荐 K=7、最大 K=15、B=1/B=8 及会改变选路的 seams 区分候选，再补足最终
routes 在实际产品域的曲线。对 state/attention 保留 W/B/context/counts 维度，对纯矩阵按
T/U 去重。只在一个 kernel 已由 benchmark 识别、且 kernel-level 答案会改变当前选择时使用
ncu；本阶段不要求尚未接入的 Engine 或 nsys 作为前提。

每项关闭时只记录相关 geometry/format、K/W/B 或列数、context/counts、选定 route、
representative latency、数值/状态检查结果和必要限制。对实现未变且证据已覆盖的项目注明
复用依据；不要求重复所有旧实验，不从 K=7 的历史数据声称 K=15 性能已通过。

### 6.3 顺序与本阶段退出条件

1. 先落实 OP-01..08 的 extent/数学合同及对应 oracle；workspace 接口与执行一起修正。
2. 优先处理会错误索引的 conv、top-k、context 和 accept，随后用完整 Op measurement
   重选重投影/head、context materialization 和 target attention/GDN/MLP 的关键 routes。
3. 完成 OP-09..32 的逐项复核和 OP-33..37 的行为验证；对未修改的小 Op 按实际风险控制证据。
4. 每项关闭后回写最终 public contract、tests、benchmark 和必要的 active reference；
   旧 exact dispatch 仅在仍为合法且有证据的内部特例时保留，移除已被替代的项目自有路径。

本阶段完成时：37 项各有明确结果；K=1..15 的实际调用域无已知合同/索引/workspace 缺口；
新增或修改的数值/状态 routes 通过独立 oracle；关键 route 选择有 Op 级性能依据。入口的
固定七步 validation、Program 资源和提交/Graph 接线由 support plan 跟进。此时可以进入
正式 Engine 集成，但不能宣告 DFlash2 端到端产品或任何新 K 的 acceptance 已完成。

当前已关闭 OP-01..03，其余项待完成；本文不保留旧 checklist 的完成表或固定七步的优化结论。完成此次
治理与后续 Engine 接入后，稳定合同回归既有 active references，删除临时 checklist。
